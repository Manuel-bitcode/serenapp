import { IonContent, IonPage } from '@ionic/react';

/** RF6/RF7/RNF5 — stub; lo implementa el módulo de ajustes. */
const SettingsPage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">Ajustes — en construcción</IonContent>
  </IonPage>
);

export default SettingsPage;
