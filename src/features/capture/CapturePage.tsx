import { IonContent, IonPage } from '@ionic/react';

/** RF2 — stub; lo implementa el módulo de captura. */
const CapturePage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">Captura — en construcción</IonContent>
  </IonPage>
);

export default CapturePage;
