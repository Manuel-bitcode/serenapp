import { IonContent, IonPage } from '@ionic/react';

/** RF1 — stub; lo implementa el módulo táctil. */
const TouchPage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">Siente — en construcción</IonContent>
  </IonPage>
);

export default TouchPage;
