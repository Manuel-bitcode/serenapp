import { IonContent, IonPage } from '@ionic/react';

/** RF4/RF5 — stub; lo implementa el módulo de escritura. */
const WritePage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">Escribe — en construcción</IonContent>
  </IonPage>
);

export default WritePage;
