import { IonContent, IonPage } from '@ionic/react';

/** RF3/RF5 — stub; lo implementa el módulo de historial. */
const HistoryPage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">Historial — en construcción</IonContent>
  </IonPage>
);

export default HistoryPage;
