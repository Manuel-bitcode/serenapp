import { IonContent, IonPage } from '@ionic/react';

/** Detalle de una entrada — stub; lo implementa el módulo de historial. */
const EntryDetailPage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">Detalle — en construcción</IonContent>
  </IonPage>
);

export default EntryDetailPage;
